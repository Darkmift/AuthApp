{% extends 'templates/app.twig'%}

{% block content %}

    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <div class="panel panel-default ">
                <div class="panel-heading">
                    {% if auth.check %}
                        Welcome  {{ auth.user.name }}!
                    {% else %}
                        <br>
                        Who dis?U hackerz?!
                    {% endif %}
                </div>
                <div class="panel-body panel-flexfix">
                    <img src=" {{ base_url()}}/../resources/images/{{ auth.user.id }}.jpg" class="img2 img-circle" alt="user image">
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}